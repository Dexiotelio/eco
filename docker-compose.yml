version: '3.9'

services:
  db:
    image: postgres:16.4-alpine3.20
    container_name: postgres-db
    environment:
      POSTGRES_USER: pepenacho
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: eco_db
    ports:
      - "5431:5431"
    networks:
      - app-network
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spring-boot-microservices-app
    ports:
      - "8080:8080"
    depends_on:
      - db
    networks:
      - app-network
    volumes:
      - app-data:/data

networks:
  app-network:
    driver: bridge

volumes:
  db-data:
    driver: local
  app-data: